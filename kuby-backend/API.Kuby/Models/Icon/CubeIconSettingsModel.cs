namespace API.Kuby.Models.Icon;

public class CubeIconSettingsModel
{
    IFormFileCollection Files = new FormFileCollection();

    //TODO: if the db stores cube settings then we need more properties
}
